# 音節規則更新（2026-09-12）

- 母音只有 `a e i o u ʉ é ɨ`，以母音數量估計音節數，不強制音節數等於母音數。
- 母音後緊接的 `y/w/n/ng` 接到該音節，並立即結束音節。例如 `aya → ay-a`、`anga → ang-a`、`angt → ang-t`。
- `ng` 是不可拆開的單位，獨立出現時聲韻母為 `0c,ng`。
- 保留原有「非緊接母音的 n/ng 可形成子音音節結尾」規則，例如 `nka → n-ka`、`ngay → ng-ay`。
- `y/w` 在音節開頭仍可接母音，例如 `ya`、`wa`。它們和 `x` 都不計入母音數。
- 其他母音之間沿用保留最後一個子音給下一音節的拆法；相鄰母音分開。韻尾後剩下的無母音片段獨立保留。
- 空白與連字號是分隔界線。主流程輸出小寫；字典統計保留原始大小寫。
- 對齊後，落單且實際拼寫以 `y/w` 開頭的音節，若前一個音節已匹配，優先向前合併；沒有前方對象則沿用原有候選處理。此處是對「落單音節」的實作解讀。

`Ailgn_syllables/Syllable_decomposition01.py` 是共同拆音節來源，`fm_dict/syllabify_word.py` 也使用它。

中文字典改用 `Ailgn_syllables/cedict_parsed_custom.json`，內容是使用者提供檔案的完整副本。主流程與 LOOCV 使用同一檔名設定；字典中的自訂聲韻母欄位直接保留。

已重新產生 16 族的 `Ailgn_syllables/Aligned_Results`、`fm_dict/音節切割結果`、`fm_dict/音節統計結果`。

2026-09-12 後續依使用者要求重跑 `Refined_Excel`、投票、排除本族的全域統計、權重合併及 `LOOCV.py`。另修正 `vote.py`：顯示順序清單不再排除自訂字典新增的聲韻母（例如 `yuan`、`wang`、`u:`）。

新版 `LOOCV_Results.xlsx` 包含各族正確率和逐詞比對。428 筆輸入中，409 筆納入測試、19 筆因中文字數與對齊列數不同而沿用既有規則略過。整詞正確 150/409（36.67%）、聲母 983/1143（86.00%）、韻母 778/1143（68.07%）。`LOOCV_run_summary.json` 記錄各族結果、舊版總計、略過清單與備份位置。新舊測試分母不同，百分比變化不能直接視為同一測試集的提升。

這次沿用現有 LOOCV 候選與排序方法，沒有重新設計實驗。其他未被主流程產生的舊版驗證報表（例如 `LOOCV_Results_CharLevel.xlsx`、`LOO_Validation_Results.xlsx` 與 `16族最終權重結果_LOO測試`）未重算。

驗證指令（於 LCS處理 目錄）：

```powershell
py -3 -m unittest discover -s Phonetic_Alignment/tests -v
```

測試包括指定母音、韻尾、ng、子音殘段、y/w 落單合併、新字典，以及 123,044 筆字典詞條的字母保留與兩個入口一致性。這些檢查驗證程式行為，並非人工標注的音節正確率。

原始程式與重算前輸出備份於專案根目錄 `backup_syllable_update_20260912_204629`。
